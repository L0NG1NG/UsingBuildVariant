pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://linphone.org/maven_repository"
        }
    }
}
rootProject.name = "UsingBuildVariant"
include ':app', ':liblinphone-sdk'
